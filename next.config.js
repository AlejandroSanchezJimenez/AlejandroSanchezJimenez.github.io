/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/AlejandroSanchezJimenez',
  output: 'export',
  distDir: 'dist',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
